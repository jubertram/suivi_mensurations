<%= simple_form_for (phase) do |f| %>
    <%= f.input :phase_type, collection: Phase::PHASE_TYPE, selected: 'Prise de masse' , label: "Type de phase", include_hidden: false %>
    <%= f.input :period, collection: Phase::PERIOD, label: "Durée de la phase que tu vises (en jours)"  %>
    <%= f.input :started_date, selected: Date.today %>
    <%= f.submit "C'est parti" %>
  <% end %>
